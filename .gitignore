# Separately-cloned upstream repo; not part of this project's history.
launchdarkly-auto-factory/

# Nested factory checkout in CI (separate git repo).
tool/

# Dependencies & build output
node_modules/
dist/
*.tsbuildinfo

# Env & local
.env
.env.local

# OS
.DS_Store
